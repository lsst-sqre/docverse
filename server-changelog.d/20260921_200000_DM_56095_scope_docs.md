### Other changes

- `docs/keeper-sync-scope.md` is a new operations page for the keeper-sync scope: the include/exclude rule the four config fields compose into, `re.fullmatch` pattern semantics and the 100-pattern / 256-character caps, what happens to a project that falls out of scope (it stops syncing and its per-project endpoints 404, but nothing is deleted or tombstoned and re-including it resumes syncing), the preview → `PATCH` → backfill workflow a wave migration uses and why previewing first matters, and how to read each field of a scope-preview response. It is linked from `docs/index.md`.
