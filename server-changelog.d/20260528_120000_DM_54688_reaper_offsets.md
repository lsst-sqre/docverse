### Other changes

- The lifecycle arq pool's five reaper crons are now staggered onto disjoint minute slots so a horizontally scaled pool never fires two reapers at the same instant. `lifecycle_reaper` keeps the canonical `{0, 30}`; `publish_edition_reaper` moves to `{6, 36}`, `build_processing_reaper` to `{12, 42}`, `dashboard_sync_reaper` to `{24, 54}`, and `dashboard_build_reaper` to `{3, 18, 33, 48}` (its 15-minute cadence is preserved). Each reaper's effective cadence is unchanged; the offsets follow the same precedent that pins `git_ref_audit_discovery` to `minute=17`.
