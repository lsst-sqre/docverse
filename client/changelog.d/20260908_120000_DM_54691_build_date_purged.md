### New features

- `Build` gains `date_purged: datetime | None`, which reports when the build's object-store content was permanently reclaimed. It is the field to check before assuming a soft-deleted build can be restored: null means the content is still there, a timestamp means the row is a tombstone and the files are gone. The field defaults to `None`, so a response from a server that does not yet send it still parses.
