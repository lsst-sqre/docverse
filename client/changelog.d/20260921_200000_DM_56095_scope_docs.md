### Other changes

- The `KeeperSyncConfig` schema description now states the whole scope rule — included by `project_slugs` or `project_slug_patterns`, minus `exclude_project_slugs` and `exclude_project_slug_patterns`, with excludes always winning — and the `project_slugs` field description on both `KeeperSyncConfig` and `KeeperSyncConfigUpdate` records that the exclude fields still apply to the `"*"` wildcard, which previously read as if it were absolute.
