### New features

- Added optional `subject_url`, `build_url`, and `edition_url` fields to the `QueueJob` model in `docverse.client.models`. These carry the HATEOAS back-reference links from a queue job to the API resource it processes (the build for `build_processing` jobs, the edition for publish jobs), so consumers follow a link instead of reconstructing the build/edition path.
