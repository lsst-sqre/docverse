### New features

- `Project` gains an optional `date_deleted` field carrying the project's soft-deletion timestamp, `None` for a live project. A project is only returned deleted when the request asked for it with `include_deleted=true`, but the field parses either way, so a consumer can tell a deletion from a project that simply stopped appearing in the listing.
