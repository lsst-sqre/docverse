### Other changes

- `ProjectList.etag` now says plainly that it is the validator to poll with, and the only one Docverse offers: the server sends no `Last-Modified` and ignores an `If-Modified-Since`. The tag is derived from the listing watermark at full precision, so it notices a change however soon after the previous one it landed and whichever order the two writes committed in.
