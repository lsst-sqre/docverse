### Other changes

- The `DraftInactivityRule` schema description now notes that a keeper-synced edition's `date_updated` is LTD Keeper's `date_rebuilt` (or `date_created`) rather than its import time, so a draft LTD last rebuilt long ago is a candidate however recently it was synced.
