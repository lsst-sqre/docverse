### Backwards-incompatible changes

- `Project`, `Organization`, and `OrganizationSummary` gain a required `id` field carrying the resource's Crockford Base32 public identifier. Parsing a response from a Docverse server older than this release will fail validation; upgrade the server alongside the client.
